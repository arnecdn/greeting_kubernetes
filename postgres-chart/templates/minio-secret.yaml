{{/*
  The minio-credentials secret is now automatically created and managed
  by the minio-setup-job (pre-install/pre-upgrade Helm hook).

  The job connects to MinIO using root credentials, creates the backup
  bucket, generates a dedicated service account, and stores the access
  key / secret key in the minio-credentials secret.

  See: minio-setup-job.yaml, minio-setup-rbac.yaml, minio-root-secret.yaml
*/}}
